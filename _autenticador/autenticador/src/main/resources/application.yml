server:
  port: 8082 # Puerto para el autentificador

spring:
  application:
    name: autentificador-service
    
  datasource:
    url: jdbc:postgresql://localhost:5432/biblioteca_autentificador_db
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/